// save results[0].types (array) this will give what type of place the marker is which will enable filtering.

// For example I can click restraunts in London or Nightlife in Tel Aviv

// after performing split the last thing is always the country, second to last thing city. if there is more it is a region and name

// for name in trips table we are just going to use the first element after performing split(",")
