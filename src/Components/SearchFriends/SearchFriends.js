import React from 'react'


export default function SearchFriends(props) {
    //onchange to execute a database regex search and send the current results back
    //map through results
    const { textVal, handleChange, searchResults } = props;
    console.log(props)
    return (
        <div>
            {/* map friends */}

        </div>
    )
}
